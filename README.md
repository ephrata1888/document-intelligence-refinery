# Document Intelligence Refinery

Multi-stage agentic pipeline for enterprise document extraction.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate  # or .venv\Scripts\activate on Windows
pip install -e .